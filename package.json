{
	"name": "music-recommendation",
	"version": "1.0.0",
	"scripts": {
		"start:frontend": "cd frontend && pnpm dev",
		"start:backend": "cd backend && go run main.go",
		"docker:up": "docker-compose up -d --build",
		"docker:down": "docker-compose down",
		"prepare": "cd frontend && pnpm install"
	}
}
